Place your Vosk model zip here as `vosk-model.zip`.
The app will unpack it to `filesDir/vosk-model` on first use.
Ensure the zip contains the model files directly (e.g., model.conf, am/, graph/), not another nested zip.
