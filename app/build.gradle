plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    id("kotlin-kapt")
}

android {
    namespace = "com.sentinel.ai"
    compileSdk = 34

    defaultConfig {
        applicationId = "com.sentinel.ai"
        minSdk = 29
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"

        // Provide your Google Speech-to-Text API key via -PSTT_API_KEY=... or in gradle.properties/local.properties
        def sttKey = (project.findProperty("STT_API_KEY") ?: "").toString().replace("\"", "")
        buildConfigField("String", "STT_API_KEY", "\"${sttKey}\"")
        // OpenAI Whisper transcription key
        def openaiKey = (project.findProperty("OPENAI_API_KEY") ?: "").toString().replace("\"", "")
        buildConfigField("String", "OPENAI_API_KEY", "\"${openaiKey}\"")
        // Self-hosted Whisper.cpp API key (X-API-Key header). Override with -PWHISPER_CPP_API_KEY or in gradle.properties/local.properties
        def whisperCppKey = (project.findProperty("WHISPER_CPP_API_KEY") ?: "D0yoUkn0wwho@mI%cAtCHm31fUcan").toString().replace("\"", "")
        buildConfigField("String", "WHISPER_CPP_API_KEY", "\"${whisperCppKey}\"")
        // IPinfo token (for country lookup). Provide via -PIPINFO_TOKEN=... or in gradle.properties/local.properties
        def ipinfoToken = (project.findProperty("IPINFO_TOKEN") ?: "b0d703ec7cfe55").toString().replace("\"", "")
        buildConfigField("String", "IPINFO_TOKEN", "\"${ipinfoToken}\"")
        // External phone validation API key (e.g., AbstractAPI/numverify). Provide via -PPHONE_REP_API_KEY=... or in gradle.properties/local.properties
        def phoneRepKey = (project.findProperty("PHONE_REP_API_KEY") ?: "522387e25611ffb108e7b3bca1c6b98c").toString().replace("\"", "")
        buildConfigField("String", "PHONE_REP_API_KEY", "\"${phoneRepKey}\"")
        // Fallback phone validation API key. Provide via -PPHONE_REP_API_KEY_FALLBACK=... or in gradle.properties/local.properties
        def phoneRepFallbackKey = (project.findProperty("PHONE_REP_API_KEY_FALLBACK") ?: "45c6c113bfe980261b6bec39aa4e11b7").toString().replace("\"", "")
        buildConfigField("String", "PHONE_REP_API_KEY_FALLBACK", "\"${phoneRepFallbackKey}\"")
        // CapSolver token (for Turnstile solving). Provide via -PCAPSOLVER_API_KEY=...
        def capsolverKey = (project.findProperty("CAPSOLVER_API_KEY") ?: "CAP-D5903264575AE0571295C9D7A21B097F").toString().replace("\"", "")
        buildConfigField("String", "CAPSOLVER_API_KEY", "\"${capsolverKey}\"")
        // Blacklistseller credentials (optional override)
        def blsUser = (project.findProperty("BLS_USER") ?: "67sixseven").toString().replace("\"", "")
        def blsPass = (project.findProperty("BLS_PASS") ?: "67766776_").toString().replace("\"", "")
        buildConfigField("String", "BLS_USER", "\"${blsUser}\"")
        buildConfigField("String", "BLS_PASS", "\"${blsPass}\"")
        // Blacklist API (smarthomeus3r)
        def blacklistApiKey = (project.findProperty("BLACKLIST_API_KEY") ?: 'k3yb11acklist$eller-aslwisdn435hu234iohh').toString().replace("\"", "")
        def blacklistApiUrl = (project.findProperty("BLACKLIST_API_URL") ?: "https://blacklist.smarthomeus3r.space/search").toString().replace("\"", "")
        buildConfigField("String", "BLACKLIST_API_KEY", "\"${blacklistApiKey}\"")
        buildConfigField("String", "BLACKLIST_API_URL", "\"${blacklistApiUrl}\"")

        testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled = false
            shrinkResources = false
            proguardFiles(
                    getDefaultProguardFile("proguard-android-optimize.txt"),
                    "proguard-rules.pro"
            )
        }
        debug {
            minifyEnabled = false
            shrinkResources = false
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = "17"
    }
    buildFeatures {
        viewBinding = true
        buildConfig = true
    }
    packaging {
        resources {
            excludes += ["META-INF/AL2.0", "META-INF/LGPL2.1"]
        }
    }
}

dependencies {
    implementation("androidx.core:core-ktx:1.12.0")
    implementation("androidx.appcompat:appcompat:1.6.1")
    implementation("com.google.android.material:material:1.10.0")
    implementation("androidx.activity:activity-ktx:1.8.2")
    implementation("androidx.constraintlayout:constraintlayout:2.1.4")
    implementation("androidx.lifecycle:lifecycle-viewmodel-ktx:2.6.2")
    implementation("androidx.lifecycle:lifecycle-livedata-ktx:2.6.2")
    implementation("androidx.core:core-splashscreen:1.0.1")
    implementation("androidx.recyclerview:recyclerview:1.3.2")
    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.7.3")
    implementation("com.squareup.okhttp3:okhttp:4.12.0")
    implementation("io.michaelrocks:libphonenumber-android:8.13.38")
    implementation("com.alphacephei:vosk-android:0.3.47")

    def room_version = "2.6.1"
    implementation("androidx.room:room-runtime:$room_version")
    implementation("androidx.room:room-ktx:$room_version")
    kapt("androidx.room:room-compiler:$room_version")

    // ML Kit Barcode Scanning for QR codes
    implementation("com.google.mlkit:barcode-scanning:17.2.0")

    // CameraX for camera preview
    def camerax_version = "1.3.1"
    implementation("androidx.camera:camera-core:$camerax_version")
    implementation("androidx.camera:camera-camera2:$camerax_version")
    implementation("androidx.camera:camera-lifecycle:$camerax_version")
    implementation("androidx.camera:camera-view:$camerax_version")

    testImplementation("junit:junit:4.13.2")
    androidTestImplementation("androidx.test.ext:junit:1.1.5")
    androidTestImplementation("androidx.test.espresso:espresso-core:3.5.1")

    // ONNX runtime for Silero VAD
    implementation("com.microsoft.onnxruntime:onnxruntime-android:1.17.0")
    debugImplementation("ai.djl.huggingface:tokenizers:0.36.0") {
        exclude group: "net.java.dev.jna", module: "jna"
    }
}
